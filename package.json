{
  "name": "app-platform-starters",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r exec pnpm build",
    "lint": "pnpm -r exec pnpm lint",
    "test": "pnpm -r exec pnpm test",
    "format": "pnpm -r exec prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "latest",
    "turbo": "latest"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=14.0.0"
  },
  "packageManager": "pnpm@7.0.0",
  "dependencies": {
  }
}
